# SparkDash SGLang prefill 兼容修复计划

## 范围

- 目标：兼容 spark-a 当前 `Qwen3.8-Flash-Next-NVFP4` SGLang runtime 的动态 prefill 展示。
- 数据源：LLM probe 的 SGLang `/get_server_info`、`/v1/loads` 与 Prometheus `/metrics`。
- 语义：主 `prefillTps` 表示实际计算（uncached）prefill tok/s；缓存命中单独展示为 cached prefill tok/s。

## 根因

当前 SGLang 轮询在 `/get_server_info` 已提供 decode 速率时，只执行 `_applySglangPrefillSplit`。该函数仅更新 cached/uncached 两个拆分速率，不更新主 `prefillTps`。现代 SGLang 还提供带 `mode` 标签的 `sglang:realtime_tokens_total`，应使用 `prefill_compute` 与 `prefill_cache` 的累计计数器计算增量速率。

## 实现

- 在 SGLang prefill-only metrics 路径优先读取 `realtime_tokens_total` 的 `prefill_cache` / `prefill_compute` 标签。
- 复用累计计数器基线；对按日志间隔批量更新的计数器，使用两次计数实际变化之间的墙钟时间，不能直接使用 2 秒轮询时间窗。
- 真实增量后的非零速率最多保留 6 秒，避免 2 秒轮询导致视觉上一闪而过；超过窗口且无新增量时归零。
- 对旧版 `prompt_tokens_total` / `cached_tokens_total` 保留现有兼容逻辑。
- 增加最小回归样本：server-info 已提供 decode，metrics 提供 realtime prefill counter 时主 prefill 必须动态非零。

## 验证

- 运行 LlmProbe SGLang 相关测试与类型检查/构建。
- 现场复核 spark-a `/metrics` 字段形状；不重启或修改当前模型容器。
